# LGS-RPA Main Configuration
project_name: lgs_rpa
experiment_name: turkish_integration_test

# Paths
data_dir: data/
checkpoint_dir: checkpoints/
output_dir: outputs/

# Models
turkish_bert: dbmdz/bert-base-turkish-cased
model_max_length: 512

# Training
batch_size: 8
learning_rate: 2e-5
num_epochs: 3

# Hardware
device: mps  # Apple Silicon GPU
